#include <c-.h>

int main() {
  Vec<int>*% xs = Vec<int>.new();

  xs.push(10);
  xs.push(20);
  xs.push(30);

  int sum = 0;                                       foreach (int x in xs) {
      sum += x;
  }
  printf("%d\n", sum); // 60
  return 0;
}
