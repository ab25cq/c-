#include <stdio.h>
#include <stdlib.h>
#include <c-.h>

int main(void)
{
    struct OwnedVec<int*>* a = OwnedVec<int*>.new();

    a.push(new int);

    *a[0] = 123;
    printf("%d\n", *a[0]);

    /* 範囲外アクセスはパニックする */
    printf("%d\n", *a[1]);

    return 0;
}
