#include <c-.h>

struct Data
{
    int a, b;
};

int main(void)
{
    int v[2] = {
        1,2
    };
    
    Span<struct Data> s = Span<struct Data>.map_from(v, sizeof(v), 1);
    
    Box<char*> a;
    
    printf("%d %d\n", (*s).a, (*s).b);
    return 0;
}
