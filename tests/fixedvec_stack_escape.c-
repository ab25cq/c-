FixedVec<int> make_vec(void)
{
    int raw[2];
    FixedVec<int> values = FixedVec<int>.from(raw, 2);

    values.push(1);
    return values;
}

int main(void)
{
    FixedVec<int> values = make_vec();

    return values.get(0);
}
