unsafe {
#include <stdio.h>
}

int main(void)
{
    Vec<int> nums = Vec<int>.new();

    nums.push(10);
    nums.push(20);

    /* The "a[0]" and 'x[1]' inside these literals must not be rewritten. */
    printf("a[0] x[1] = %d\n", nums[1]);
    printf("%c\n", 'a');

    return 0;
}
