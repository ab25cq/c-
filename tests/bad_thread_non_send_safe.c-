unsafe {
struct Work {
    int* pointer;
};
}

int consume(owned Work work)
{
    return 0;
}

int main(void)
{
    Work work;
    Thread thread;

    thread = Thread.spawn(move work, consume);
    return thread.join();
}
