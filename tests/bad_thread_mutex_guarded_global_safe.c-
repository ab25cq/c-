int shared_value;
Mutex gate;

int worker(void)
{
    gate.lock();
    shared_value = shared_value + 1;
    gate.unlock();
    return 0;
}

int main(void)
{
    Thread thread;

    thread = Thread.spawn(worker);
    thread.join();
    return 0;
}
