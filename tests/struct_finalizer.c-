#include <stdlib.h>

struct Child {
    int value;
};

struct Holder {
    Box<Child> child;
};

int main(void)
{
    Box<Holder> stack = new Holder;

    Box<Holder> heap = new Holder;

    return stack != NULL && heap != NULL ? 0 : 1;
}
