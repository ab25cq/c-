#include <stdlib.h>

struct Child {
    int value;
};

struct Holder {
    Child child;
};

int main(void)
{
    Holder stack = new Holder;

    Holder heap = new Holder;

    return stack != NULL && heap != NULL ? 0 : 1;
}
