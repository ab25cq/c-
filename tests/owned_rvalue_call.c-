unsafe {
#include <string.h>
}

string x(void)
{
    string out = s"aaa";
    return move out;
}

int main(void)
{
    unsafe {
        char* p = x() + 1;
        if (p == NULL) {
            return 1;
        }
    }
    string text = x();
    if (text == NULL) {
        return 1;
    }
    if (x()) {
        return 0;
    }
    return 0;
}
