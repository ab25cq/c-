Mutex gate;

int unlock_from_worker(void)
{
    gate.unlock();
    return 0;
}

int main(void)
{
    Thread thread;

    gate.lock();
    thread = Thread.spawn(unlock_from_worker);
    while (1) {
        Thread.yield();
    }
    return 0;
}
