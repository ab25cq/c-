#include <stdlib.h>
#include <string.h>

struct Data {
    owned char* text;
};

int main(void)
{
    struct Data data;

    data.text = strdup("aaa");
    data.text = strdup("bbb");
    if (strcmp(data.text, "bbb") != 0) {
        return 1;
    }
    return 0;
}
