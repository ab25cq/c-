unsafe {
#include <string.h>
}

struct Data {
    string text;
};

int main(void)
{
    Data data = new Data;

    data->text = s"aaa";
    data->text = s"bbb";
    if (!data.text.eq("bbb")) {
        return 1;
    }
    return 0;
}
