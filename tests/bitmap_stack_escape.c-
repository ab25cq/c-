Bitmap make_bitmap(void)
{
    unsigned long words[1];
    Bitmap bits = Bitmap.from_words(words, 1);

    bits.set(0);
    return bits;
}

int main(void)
{
    Bitmap bits = make_bitmap();

    return bits.test(0);
}
