Span<int> make_view(void)
{
    int raw[2] = { 1, 2 };
    Span<int> values = Span<int>.from(raw, 2);

    return values;
}

int main(void)
{
    Span<int> values = make_view();

    return values.get(0);
}
