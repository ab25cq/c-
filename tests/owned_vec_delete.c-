unsafe {
#include <stdio.h>
#include <stdlib.h>
}

int main(void)
{
    OwnedVec<int*> a = OwnedVec<int*>.new();
    unsafe {
        int* value = calloc(1, sizeof(int));

        *value = 42;
        a.push(move value);
    }
    printf("%d\n", a.len());

    return 0;
}
