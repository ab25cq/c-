struct Pair {
    int x;
};

Box<Pair> make_pair(void);

Box<Pair> make_pair(void)
{
    Box<Pair> p = new Pair;
    return p;
}

int main(void)
{
    Box<Pair> p = make_pair();
    return p == NULL ? 1 : 0;
}
