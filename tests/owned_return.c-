#include <stdlib.h>

struct Pair {
    int x;
};

struct Pair* make_pair(void);

struct Pair* make_pair(void)
{
    struct Pair* p = malloc(sizeof(struct Pair));
    return move p;
}

int main(void)
{
    struct Pair* p = make_pair();
    return 0;
}
