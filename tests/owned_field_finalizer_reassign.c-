struct Holder {
    string text;
};

int main(void)
{
    Holder holder = new Holder;

    holder->text = s"aaa";
    holder->text = s"bbb";

    if (holder->text == NULL) {
        return 1;
    }
    return 0;
}
