unsafe {
#include <string.h>
}

struct Person {
    string name;
    int age;
};

int main(void)
{
    Box<Person> person = new Person { name: s"aaa", age: 48 };

    if (!person.name.eq("aaa")) {
        return 1;
    }
    if (person->age != 48) {
        return 2;
    }
    return 0;
}
