unsafe {
#include <string.h>
}

struct Person {
    string name;
    int age;
};

int main(void)
{
    Person person = new Person { name: s"aaa", age: 48 };

    if (strcmp(person->name, "aaa") != 0) {
        return 1;
    }
    if (person->age != 48) {
        return 2;
    }
    return 0;
}
