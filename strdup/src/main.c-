#include <stdio.h>
#include <stdlib.h>
#include <c-.h>

int main(void)
{
    struct OwnedList<int*>* li = OwnedList<int*>.new();
    
    int* n = new int;
    
    li.push(move n);
    li.push(move n);
    
    return 0;
}
