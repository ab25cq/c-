#include <stdio.h>
#include <stdlib.h>
#include <c-.h>

int main(void)
{
    struct OwnedList<int*>* li = OwnedList<int*>.new();
    
    int* n = calloc(1, sizeof(int));
    int* n2 = calloc(1, sizeof(int));
    
    li.push(move n);
    li.push(move n2);
    
    return 0;
}
